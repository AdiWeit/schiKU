name: Update Website

on:
  push:
    branches:
      - 'main'

jobs:
  copy-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          path: 'src'
          ref: 'main'
          persist-credentials: false

      - name: Checkout
        uses: actions/checkout@v3
        with:
          path: 'dest'
          ref: 'main'
          repository: 'AdiWeit/my_website'
          token: ${{ secrets.WEBSITE_TOKEN }}
          # fetch-depth: 0
          persist-credentials: true
      - name: Copy files to new repo
        shell: bash
        run: cp -rvT src/ dest/SchiKU
      - name: Check for new files
        id: newFiles
        shell: bash
        working-directory: dest
        run: |
          git add -A
          git status --porcelain | wc -l
          if [[ $(git status --porcelain | wc -l) -gt 0 ]]; then
            echo "hasNewFiles=true" >> $GITHUB_OUTPUT
          else
            echo "hasNewFiles=false" >> $GITHUB_OUTPUT
          fi
      - name: show variable output
        run: echo "hasNewFiles='${{ steps.newFiles.outputs.hasNewFiles }}'"
      - name: Push new files
        if: ${{ steps.newFiles.outputs.hasNewFiles == 'true' }}
        shell: bash
        working-directory: dest
        run: |
          git config --global user.name "AdiWeit"
          git config --global user.email "43521682+AdiWeit@users.noreply.github.com"
          git commit -m "Updated SchiKU files from repository other"
          git push
